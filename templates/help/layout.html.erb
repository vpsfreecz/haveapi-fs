<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title><%= @c.class %></title>
        <link rel="stylesheet" href="<%= asset('css/bootstrap.min.css') %>">
        <link rel="stylesheet" href="<%= asset('css/local.css') %>">
    </head>
    <body>
        <div class="container-fluid">
            <div class="row">
                <div class="col-md-8 col-md-offset-2">
                    <div class="page-header">
                        <h1><%= @c.title %> <small>[<a href=".">browse</a>]</small></h1>
                    </div>
                    <ol class="breadcrumb">
                        <% @c.context.object_path[1..-1].each do |k, v| %>
                            <li><a href="<%= v.abspath %>/help.html"><%= v.title %></a></li>        
                        <% end -%>
                    </ol>
                    <div class="panel panel-default">
                        <div class="panel-body">
                            <%= yield %>
                        </div>

                        <div class="panel-footer text-right">
                            Generated by HaveAPI FUSE v<%= HaveAPI::Fs::VERSION %>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </body>
</html>
